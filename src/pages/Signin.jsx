import { Link } from "react-router-dom";

const Signin = () => {
  return (
    <div>
      <h1>Sign In</h1>
      <Link to="/">Don't have an account? Sign Up</Link>
    </div>
  );
};

export default Signin;
